import ImageItemController from './controller'

/**
 * @param {import('express').Router} router 
 */
function run(router) {
    router.get('/image-item', (req, res) => {
        
    })

    router.post('/image-item', (req, req) => {
        
    })
}

export default run