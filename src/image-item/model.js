export default class {
    /**
     * @param {string} id 
     * @param {string} vendorID 
     * @param {string} name 
     */
    constructor(id, vendorID, name) {
        this.id = id
        this.vendorID = vendorID
        this.name = name
    }
}